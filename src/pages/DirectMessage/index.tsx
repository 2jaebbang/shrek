import Workspace from 'components/Workspace';
import React from 'react';

const DirectMessage = () => {
  return (
    <Workspace>
      <div>다이렉트 메시지</div>
    </Workspace>
  );
};

export default DirectMessage;
